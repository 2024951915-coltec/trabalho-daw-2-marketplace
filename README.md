# trabalho-daw-2-marketplace

Precisa rodar o comando abaixo:

```
npm install express ejs @sequelize/core @sequelize/sqlite3 bcryptjs express-session socket.io
```

(OBS: *ao apresentar o trabalho final, vá para* `modelos/configs.js` *e defina a opção* `FORMATAR_BANCO_DE_DADOS` *para* `false`)